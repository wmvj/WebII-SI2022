// const message = "Hello TypeScript!"
let message = "Hello TypeScript!"

function showMessage() {
    console.log(message)
}

// Consigo mudar?
message = "TypeScript!"

// Consigo executar?
// message()
showMessage()

const user = {
    name: "Warley Junior",
    email: "wmvj@unifesspa.edu.br"
}

// Propriedade existe?
// console.log(user.avatar)
console.log(user.name)